$(document).ready(function(){ //why do I need this part. In your example you only used The inner funktion?
    $('.mySlider').slick({
      dots: true,
      prevArrow: '<div class="slick-prev"></div>',
      nextArrow: '<div class="slick-next"></div>'
    });
});